const CREATE = 'CREATE';
const READ = 'READ';
const UPDATE = 'UPDATE';
const DELETE = 'DELETE';
const LIST = 'LIST';

const types = {
  CREATE,
  READ,
  UPDATE,
  DELETE,
  LIST,
};

export default types;